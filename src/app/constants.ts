export const COLS = 50; // needs to be evenish for starting position
export const ROWS = 80;
export const BLOCK_SIZE = 10; // needs to be even.
export const STEP_SIZE = 1;
export const MAX_POINTS_LEVEL = 100;